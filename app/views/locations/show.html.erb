<h1>Welcome to <%= @location[:name] %></h1>
<div id="city-splash">

</div>
<% if @location_items.nil? %>
  <h2>We're sorry but there are currently no items for this location right now</h2>
<% else %>
  <h2>Items for this location: </h2>
  <% @items.each do |item| %>
  <ul><%= item[:name] %></ul>
  <ul><%= item[:location] %></ul>
  <% end %>
<% end %>
